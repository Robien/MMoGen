package Game;

message MessageType
{
	enum EMessageType
	{
		PING = 1;
		PONG = 2;
	}
	required EMessageType type = 1;
	
	optional Ping ping = 2;
	optional Pong pong = 3;
}

message Ping
{
	required uint32 id = 1;
}

message Pong
{
	required uint32 id = 1;
}
